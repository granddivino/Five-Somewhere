<!DOCTYPE html>
<link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Shadows+Into+Light&display=swap" rel="stylesheet">
<html lang="en">
<style>
    h1 {
        text-align: center;
        font-size: 7vw;
        border-radius: 10px;
        background-color: #ecdddd;
        opacity: 0.9; 
    }
    body {
        font-family: 'Shadows Into Light', cursive;
        font-size: 3vw;
        background: url(https://static4.depositphotos.com/1005549/361/v/950/depositphotos_3610246-stock-illustration-rainbow-cocktails.jpg);
    }

    #loginButton, #logoutButton {
     line-height: 40px;
     width: 150px;
     font-family: 'Shadows Into Light', cursive;
     font-size: 3vw;
     margin-top: 4px;
     margin-right: 6px;
     position:absolute;
     top:0;
     right:0;
    }

    #signupButton, #favoritesButton {
     line-height: 40px;
     flex-wrap: wrap;
     font-family: 'Shadows Into Light', cursive;
     font-size: 3vw;
     margin-top: 4px;
     margin-right: 200px;
     position:absolute;
     top:0;
     right:0;
    }
    

</style>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project 2</title>
</head>
<body>
    <header>
        <h1>Five o' Clock Somewhere</h1>
        <nav>
            <ul>
                <% if (!currentUser) { %>
                    <div id="signupButton">
                        <a href="/auth/signup">Sign Up</a>
                    </div>
                    <div id="loginButton">
                        <a href="/auth/login">Log In</a>
                    </div>
                <% } else { %>
                    <div id="logoutButton">
                        <a href="/auth/logout">Log Out</a>
                    </div>
                    <div id="favoritesButton">
                        <a href="/favorites/drinks">Favorites</a>
                    </div>
                <% } %>
            </ul>
        </nav>
    </header>
    <%- include('partials/alerts.ejs') %>
    <%- body %>
</body>
</html>